var url = "/api";

/**
 * An API controller handles the request and the response 
 * Reacts like an REST API
 */
class BaseAPI{

    static getUrl(){
        return url;
    }

}

module.exports = BaseAPI;